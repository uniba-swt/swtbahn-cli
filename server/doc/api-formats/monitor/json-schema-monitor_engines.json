{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "monitor_engines.schema.json",
  "title": "engines",
  "description": "List of train engine (i.e., behaviour models, not physical trains/locomotives) identifiers",
  "type": "object",
  "properties": {
    "engines": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "engines"
  ]
}