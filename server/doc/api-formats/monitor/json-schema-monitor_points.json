{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "monitor_points.schema.json",
  "title": "points",
  "description": "list of points, consisting of the point identifier, its state, and the target_state_reached status if available.",
  "type": "object",
  "properties": {
    "points": {
      "type": "array",
      "uniqueItems": true,
      "minItems": 0,
      "items": {
        "required": [
          "id",
          "state"
        ],
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1
          },
          "state": {
            "type": "string",
            "minLength": 0
          },
          "target_state_reached": {
            "type": "boolean",
            "$comment": "This is optional, as DCC-accessories (so possibly points) don't provide this info. True if execution state is reached or reached_verified, otherwise false."
          }
        }
      }
    }
  },
  "required": [
    "points"
  ]
}